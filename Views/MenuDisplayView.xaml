<Window x:Class="RestaurantApp.Views.MenuDisplayView"         
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"         
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"         
        xmlns:vm="clr-namespace:RestaurantApp.ViewModels"         
        Title="Meniu restaurant" Height="600" Width="800">
    <Window.DataContext>
        <vm:MenuDisplayVM/>
    </Window.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding MenuByCategory}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10,10,10,5">
                        <TextBlock Text="{Binding Key}"                                    
                                   FontSize="18" FontWeight="Bold"                                    
                                   Foreground="#FF2C3E50"                                    
                                   Margin="0,0,0,5"/>
                        <Separator Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding Value}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#FFCCCCCC"                                             
                                            BorderThickness="1"                                             
                                            CornerRadius="5"                                             
                                            Margin="0,0,0,6"                                             
                                            Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Name}"                                                     
                                                           FontSize="14"                                                     
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding DisplayQuantity}" 
                                                           Foreground="#FF666666" FontSize="12"/>
                                                <TextBlock Text="INDISPONIBIL"                                                     
                                                           Foreground="Red"                                                     
                                                           FontWeight="Bold"                                                    
                                                           FontSize="12"                                                     
                                                           Visibility="Collapsed">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                            <Image Grid.Column="1" Width="Auto" Height="30" Margin="10,0"
                                                   Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}"/>
                                            <TextBlock Grid.Column="2"                                                 
                                                       Text="{Binding Price, StringFormat={}{0:F2} lei}"                                                 
                                                       FontSize="14"                                                 
                                                       FontWeight="Bold"                                                 
                                                       Foreground="#FF27AE60"                                                 
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Window> 
