<Window x:Class="RestaurantApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RestaurantApp.ViewModels"
        Title="Autentificare" Height="240" Width="400"
        Loaded="Window_Loaded">
    <Window.DataContext>
        <vm:LoginVM />
    </Window.DataContext>
    <StackPanel Margin="20">
        <WrapPanel Margin="0,10,0,10">
            <Label Width="120" Content="Email:" FontSize="14"/>
            <TextBox Width="220" FontSize="14"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        </WrapPanel>
        <WrapPanel Margin="0,0,0,10">
            <Label Width="120" Content="Parola:" FontSize="14"/>
            <PasswordBox Name="pbPassword" Width="220" FontSize="14"/>
        </WrapPanel>
        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red" FontSize="13"
                   Margin="0,5,0,5"/>
        <Button Content="Autentifica-te" Height="35" FontSize="14"
                Command="{Binding LoginCommand}">
            <Button.CommandParameter>
                <Binding Path="Password" ElementName="pbPassword"/>
            </Button.CommandParameter>
        </Button>
    </StackPanel>
</Window>
